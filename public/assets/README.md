# Portfolio assets

The three `orbit-*.svg` files are exact exports from the supplied Figma design.

When the final media is available, add it here using these suggested names:

- `portrait.png` — Kirushnavy's supplied portrait (the site crops it automatically inside the profile frame)
- `study-assistant.webp` — Academic Study Assistant project cover
- `weather-app.webp` — Weather App project cover
- `git-assignment.webp` — Git & GitHub Assignment project cover
- `kirushnavy-yogathas-resume.pdf` — downloadable résumé

The current interface intentionally shows the Figma placeholders until those files are supplied.
